# Photos

Dépose ici les photos à partager (JPG/PNG/WebP). Puis ajoute le nom de chaque fichier dans `photos.json` (tableau de chaînes), par exemple :

```json
["photo1.jpg", "photo2.jpg"]
```

Elles apparaissent automatiquement dans l'onglet "Photos" du site, dans l'ordre du tableau.
